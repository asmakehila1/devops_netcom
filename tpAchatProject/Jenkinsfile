pipeline {
   agent any

   stages {
        stage('clean') {
              steps{
                dir("${env.WORKSPACE}/tpAchatProject"){
                    sh "pwd"
                    sh 'mvn clean'
                }
            }
       }
      
   }

}